import UIKit

// Xcode requires a compilable source target before the post-compile script
// replaces the executable with the Rust/Slint binary.
@main
final class Bootstrap: UIResponder, UIApplicationDelegate {}
